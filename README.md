# Daily Obs Show（從 Obsidian Daily 產生可瀏覽的 HTML）

最簡流程：輸入起訖日期 → 直接產出含多天內容的 HTML（檔名帶日期區間），可在手機/電腦直接開啟檢視（file://）。

## 專案結構（精簡）
```
.
├── index.html                     # 模板頁（會被嵌入 JSON）
├── index-YYYYMMDD-YYYYMMDD.html   # 每次產生的最終頁（例：index-20251009-20251017.html）
├── data.json                      # 產生時的資料快照（除錯/對照用）
├── css/
│   └── style.css
├── js/
│   └── app.js
├── images/
│   └── 1000_assets/               # 可選：若要把圖複製到站內，可放這裡
├── generate_site.py               # 主腳本：輸入日期 → 內嵌 JSON → 輸出最終 HTML
├── parse_daily.py                 # 單日 .md 解析器（被主腳本呼叫）
└──py_images.py                 # 可選：把圖片從 Obsidian vault 複製到 images/1000_assets
```

## 需求
- Python 3.10+
- Obsidian Daily 目錄（預設：`L:\我的雲端硬碟\GD_ObsidianVault\02_Daily`）
- 附件目錄（若要複製圖片，預設：`L:\我的雲端硬碟\GD_ObsidianVault\1000_assets`）

> 若你的路徑不同，可在執行時用參數覆蓋。

## 一步到位：產生多天 HTML
在專案根目錄執行（Windows 範例）：

```bash
# 產生 2025-10-09 ~ 2025-10-17 的頁面
python generate_site.py --start 10-09 --end 10-17
# 或指定完整年-月-日
# python generate_site.py --start 2025-10-09 --end 2025-10-17
```

執行後會：
- 掃描 `02_Daily/` 內對應的 `.md`，解析為多天 JSON
- 寫入 `data.json`
- 將 JSON 內嵌到 `index.html` 的 `<script id="data-json">` 中
- 輸出 `index-YYYYMMDD-YYYYMMDD.html`（可直接雙擊開啟檢視）

頁面特性：
- 單欄 RWD（手機/平板/桌機）
- 每天一組卡片（💤 睡覺時間、🏃‍♀️ 運動〈無則隱藏〉、🍎 飲食〈文字〉、🖼️ 照片〈可左右滑動〉）
- 左側浮動 TOC 可跳日期
- 分頁標題與頁首顯示日期區間（起日 ~ 迄日）

### 顯示規則（提示徽章）
- 晚睡：
  - 規則：若任一「睡覺時間」的起始時間在 00:00 之後（00:00–05:59），於「💤 睡覺時間」標題後顯示「❌ 晚睡」。
  - 說明：不再依賴核取方塊，直接以入睡時間判斷；時間格式支援 `HH:MM` 或 `HHMM`。
- 八點後進食：
  - 規則：若任一飲食紀錄時間 ≥ 20:00，於「🍎 飲食」標題後顯示「❌ 八點後進食」。
  - 說明：時間格式支援 `HH:MM` 或 `HHMM`。

## 圖片顯示說明
預設顯示策略：
1) 先嘗試站內 `images/1000_assets/<檔名>`
2) 若無，前端會嘗試 `file:///L:/我的雲端硬碟/GD_ObsidianVault/1000_assets/<檔名>`（僅本機可見）

若要把圖「複製進站內」以便分享：
```bash
python copy_images.py --vault-assets "L:\我的雲端硬碟\GD_ObsidianVault\1000_assets"
```
這會把 `data.json` 中引用的圖片複製到 `images/1000_assets/`。

## CLI options
- generate_site.py:
```bash
python generate_site.py --start 10-09 --end 10-17 \
  --vault-daily "L:\我的雲端硬碟\GD_ObsidianVault\02_Daily" \
  --index index.html
```
- copy_images.py:
```bash
python copy_images.py --data data.json \
  --vault-assets "L:\我的雲端硬碟\GD_ObsidianVault\1000_assets" \
  --site-images images/1000_assets
```

Note: flags are `--vault-daily` (with y) and `--vault-assets` (plural s).

## 常見問題
- 看不到圖片？
  - 檔名需與 vault 內實際檔案一致（含空白與副檔名）。
  - 若不想複製，需在同一台電腦上、保持 `L:\...\1000_assets` 路徑可存取。
- 想只看某一天？
  - 直接在 `index-YYYYMMDD-YYYYMMDD.html` 左側 TOC 點日期跳轉。
- 想改顏色/字體？
  - 編輯 `css/style.css`（如 `.day .date-title`、`.card`、`.photo-strip img`）。

## 建議最小流程
1) 產生：
```bash
python generate_site.py --start 10-09 --end 10-17
```
2) 打開 `index-20251009-20251017.html`
3) 若要分享給沒有 vault 的人，再執行圖片複製並一併提供 `images/` 目錄

—
若要加入彙總統計（例如睡眠總時數、一週運動次數）或更多欄位，告訴我想怎麼呈現，我再加上去。
